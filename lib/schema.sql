CREATE TABLE sub-genres
    (id INTEGER PRIMARY KEY,
  );

CREATE TABLE series
    (id INTEGER PRIMARY KEY,
    title TEXT,
    author_id TEXT,
    sub-genre_id TEXT
  );

CREATE TABLE series
    (id INTEGER PRIMARY KEY,
  );
